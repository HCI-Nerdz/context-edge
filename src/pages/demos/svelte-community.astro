---
import BaseLayout from '../../layouts/BaseLayout.astro';
import DemoHead from '../../components/DemoHead.astro';
import MapEdgeShell from '../../components/svelte/MapEdgeShell.svelte';
import { MAP_EDGE_DESCRIPTION, MAP_EDGE_LEDE } from '../../lib/map-edge';
---

<BaseLayout title="Svelte community · Context Edge" description={MAP_EDGE_DESCRIPTION}>
  <main class="shell">
    <DemoHead
      variant="map"
      framework="svelte"
      title="Map Edge"
      lede={MAP_EDGE_LEDE}
    />
    <MapEdgeShell client:load initialId="docs" />
  </main>
</BaseLayout>
