/** Shared Map Edge page copy — vanilla and framework implementations. */

export const MAP_EDGE_LEDE =
  'This animation reveals a navigation map underneath the current page. Like a city with streets and buildings, that bottom layer is how you travel between activities — so the page itself can stay focused on the work, not a growing suite of nav menus and top bars.';

export const MAP_EDGE_DESCRIPTION =
  'Map Edge slides the page aside to reveal a directory map already underneath.';
