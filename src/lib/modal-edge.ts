/** Shared Modal Edge page copy — vanilla and framework implementations. */

export const MODAL_EDGE_LEDE =
  'Your current page is a sheet sitting on top of where you came from. Click the top or left edge to slide this sheet back — earlier places stay underneath as tinted layers, so you can step back without hunting a back button or a stuffed suite header.';

export const MODAL_EDGE_LEDE_LIVE =
  'Same stacked-sheet idea as Still: ancestors wait underneath as colored layers. Move near the top or left edge and this sheet peeks open so you can feel the stack; click to slide it all the way back and step into a prior place.';

export const MODAL_EDGE_DESCRIPTION =
  'Modal Edge treats the current page as a sheet you can slide back to reveal tinted ancestor layers.';

export const MODAL_EDGE_DESCRIPTION_LIVE =
  'Modal Edge live mode: pointer near the edges peeks the sheet; click opens the full ancestor stack.';
