= 2026-08-10 — Path length, tint, and live modes

* Expand no longer equalizes segment lengths (rootward compression persists)
* Controls renamed to Top length / Left length; two left packs share left length
* Path modes: Rest, Tint, Live, Tint·Live
* Tint expand is monochrome; pointer mask reveals de-intensified `--seg` colors
* Map / Modal each gain a Live mode for constant pointer reaction
